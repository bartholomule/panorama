
include @top_srcdir@/Makefile.vars

FILES = ./atmosphere.rt

all:

install:
		@echo "Installing scenes (atmospheric)..."
		@@top_srcdir@/mkinstalldirs ${TOPDIR}/scenes/atmospheric
		@set -e; \
		for i in $(FILES); do \
		  @INSTALL_DATA@ $$i ${TOPDIR}/scenes/atmospheric; \
		done

clean:

distclean:
		@rm -f Makefile
		@rm -f *.tga
