
BUILD_TOP = ../..

include ${BUILD_TOP}/Makefile.vars

INSTALL = @INSTALL@
SRC_DIR = @srcdir@

FILES = projector.rt \
	warn.rt

all:

install:
		@echo "Installing scenes (lights)..."
		@${BUILD_TOP}/mkinstalldirs ${SCENE_DIR}/lights
		@set -e; \
		for i in $(FILES); do \
		  @INSTALL_DATA@ ${SRC_DIR}/$$i ${SCENE_DIR}/lights; \
		done

clean:

distclean:
		@rm -f Makefile
		@rm -f *.tga
