
include @top_srcdir@/Makefile.vars

FILES = ./f16.tga ./supernova.tga
        
all:

install:
		@echo Installing texture files...
		@set -e; \
		for i in $(FILES); do \
		  @INSTALL_DATA@ $$i ${TEXTURE_DIR}; \
		done

clean:

distclean:
		@rm -f Makefile
