
include @top_srcdir@/Makefile.vars

FILES = ./f16.tga \
        ./supernova.tga
        
all:

install:
		@echo Installing texture files...
		@@INSTALL_DATA@ ${FILES} ${TEXTURE_DIR}

clean:

distclean:
		@rm -f Makefile
