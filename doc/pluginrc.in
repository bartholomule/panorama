#
# Panorama plugin list
#

# Atmospheric objects
atm_const.so

# BSDF
bsdf_cook_torrance.so
bsdf_lambertian.so
bsdf_phong.so
bsdf_schlick.so
bsdf_schlick_double.so
bsdf_ward.so

# Cameras
parallel.so
pinhole.so
thinlens.so

# Image filters
if_cartoon.so
if_convert_to_grey.so
if_fog.so
if_lens_flare.so
@PLUGIN_TEXT@

# Image input/output formats
tga_io.so
tiff_io.so
@PLUGIN_PNG@
@PLUGIN_JPEG@
@PLUGIN_GIF@

# Lights
point_light.so
projector.so
warn_light.so

# Patterns
pat_amplifier.so
pat_brick.so
pat_checker.so
pat_composite.so
pat_crackle.so
pat_cylgrad.so
pat_leopard.so
pat_marble.so
pat_noise.so
pat_parquet.so
pat_texture.so
pat_wood.so
pat_worley.so

# Perturbations
per_brick.so
per_bump.so
per_crackle.so
per_leopard.so
per_marble.so
per_noise.so
per_wave.so
per_worley.so

# Objects
bezier.so
aggregate_uniform_grid.so

# Object filters
of_cartoon.so
of_convert_to_grey.so

# Renderers
raytracer.so

# Scene input/output formats
rt_io.so
